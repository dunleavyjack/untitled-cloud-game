import React from 'react';
import CloudContainer from '../../components/CloudContainer/CloudContainer';
import './Home.css';

const Home: React.FC = () => {
    return (
        <main className="page">
            <CloudContainer />
        </main>
    );
};

export default Home;
